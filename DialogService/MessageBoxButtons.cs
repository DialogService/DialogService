using System;
using System.Collections.Generic;
using System.Text;

namespace DialogService
{
    public enum MessageBoxButton
    {
        OK, Yes, No, Cancel, Retry, Abort
    }

    public enum MessageBoxButtons
    {
        OK,
        YesNo,
        YesNoCancel,
        RetryAbort,
    }
}
